export enum ShopStatus {
    PENDING = 'pending',
    ACTIVE = 'active',
    INACTIVE = 'inactive'
}

export enum CouponStatus {
    EXPIRED = 0,
    ACTIVE = 1
}

export enum OrderStatus {
    PENDING = 'pending',
    PAID = 'paid',
    SHIPPED = 'shipped',
    COMPLETED = 'completed',
    CANCELED = 'canceled'
}